# Services

This directory contains application services for the RetailOps Platform.

For the MVP, the platform starts with a single backend API service located in `backend-api/`.

Target architecture may later split this into domain services such as inventory, sales, forecasting, anomaly detection, recommendations, and notifications.
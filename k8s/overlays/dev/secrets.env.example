# Legacy root-level example kept only for compatibility with older local notes.
# Prefer k8s/overlays/dev/secrets/runtime-secrets.env.example.
DATABASE_URL=REPLACE_WITH_LOCAL_DATABASE_URL
POSTGRES_PASSWORD=REPLACE_WITH_LOCAL_POSTGRES_PASSWORD

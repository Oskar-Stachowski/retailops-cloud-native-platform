# Copy this file locally and replace the placeholders before applying manifests.
# Do not commit real runtime values.
DATABASE_URL=REPLACE_WITH_LOCAL_DATABASE_URL
POSTGRES_PASSWORD=REPLACE_WITH_LOCAL_POSTGRES_PASSWORD
